# Python dependencies — Render API server + local training scripts
#
# The Render build command installs this file:
#   pip install -r scripts/requirements.txt
#
# ── API runtime (required on Render) ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # required by FastAPI for file uploads
pillow>=10.0.0            # image decoding in app.py

# ── ML — model loading + local training ───────────────────────────────────────
# tensorflowjs is needed at runtime (app.py loads the TF.js layers-model).
# tensorflow + the other packages are also used by scripts/train_mobilenet.py.
tensorflow>=2.13,<3.0
tensorflowjs>=4.10.0
numpy>=1.24,<2.0
scikit-learn>=1.3.0
tqdm>=4.66.0
